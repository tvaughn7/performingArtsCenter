{
  "name": "performing-arts-center",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "watch:sass": "sass --watch scss:css",
    "build:sass": "sass scss:css --no-source-map --style=compressed"
  },
  "devDependencies": {
    "sass": "^1.74.0"
  }
}
